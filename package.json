{
  "name": "possum-finance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "goldsky subgraph deploy",
    "create-local": "graph create --node http://localhost:8020/ possum-finance",
    "remove-local": "graph remove --node http://localhost:8020/ possum-finance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 possum-finance",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.74.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "matchstick-as": "0.5.0"
  }
}